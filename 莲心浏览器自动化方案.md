# 莲心浏览器自动化升级方案

> 版本：1.0
>
> 状态：设计完成，下一阶段按本方案实施
>
> 目标：让莲心能够在用户授权范围内，稳定地观察网页、输入文字、点击元素、等待页面变化并完成多步骤浏览器任务。

## 1. 方案结论

莲心不应让大模型直接操控鼠标坐标，也不应让大模型自由编写任意 JavaScript。最合适的实现方式是：

```text
用户意图
    ↓
LLM 意图理解
    ↓
Browser Skill（浏览器技能）
    ↓
BrowserController（浏览器控制器）
    ↓
Playwright
    ↓
Chromium / Edge
```

每一个浏览器任务都遵循：

```text
观察页面 → 选择元素 → 执行动作 → 获取新状态 → 判断是否完成
```

大模型负责“理解用户想做什么、决定下一步做什么”；Playwright 和本地控制器负责“准确地找到元素并执行动作”。

本方案借鉴 `参考项目/super-agent-party-main` 的工具调用、页面快照、操作后反馈、MCP 扩展和桌面控制思想，但不会直接复制其全部 Electron、CDP、PyAutoGUI 架构。莲心已经拥有 Python Playwright 控制器，优先在现有基础上完善，比重新引入一套浏览器内核更加稳定、易测试、易部署。

## 2. 现状基线

### 2.1 莲心已有能力

当前主要实现位于：

- `brain/browser_controller.py`
- `skills/浏览器自动化/SKILL.md`
- `skills/浏览器自动化/tools.py`
- `skills/_提示词指南/browser.md`
- `brain/tool_registry.py`
- `brain/agent.py`

已有能力包括：

1. 使用 Playwright Python 绑定控制 Chromium。
2. 使用持久化浏览器配置目录保存登录状态：`~/.lianxin/browser_profile/`。
3. 使用 JavaScript 提取可交互元素。
4. 为页面元素生成短期引用，例如 `e0`、`e1`、`e2`。
5. 已有 `browser_navigate`、`browser_snapshot`、`browser_click`、`browser_fill`、`browser_screenshot` 工具。
6. 点击和填写后会返回新的页面快照。
7. 浏览器工具已经被归入同一个 `browser` 资源组，避免共享浏览器被并行调用破坏状态。

因此，莲心不是从零开始，而是需要把已有的浏览器能力升级成更加完整的“浏览器智能体执行闭环”。

### 2.2 参考项目的可借鉴部分

`super-agent-party-main` 主要提供了三种思路：

1. 内置浏览器通过 Electron + Chromium + CDP 控制。
2. 外部浏览器通过 MCP、BrowserMCP 或 Playwright MCP 暴露工具。
3. DOM 无法操作时，通过截图、UI 树、PyAutoGUI 和系统辅助功能进行桌面级兜底。

其中最值得借鉴的不是某个具体库，而是以下执行模式：

```text
take_snapshot
    ↓
模型选择 UID
    ↓
click / fill
    ↓
重新 snapshot
    ↓
继续决策
```

## 3. 总体架构

### 3.1 四层职责

```text
┌──────────────────────────────────────────────┐
│ L1 认知层：LLM / Agent                       │
│ 理解“搜索天气”“填写表单”“打开某个页面”等意图 │
└──────────────────────────────────────────────┘
                      ↓ 结构化任务
┌──────────────────────────────────────────────┐
│ L2 技能层：Browser Skill                     │
│ 管理浏览器任务循环、步骤、确认和完成判断       │
└──────────────────────────────────────────────┘
                      ↓ 工具调用
┌──────────────────────────────────────────────┐
│ L3 控制层：BrowserController                  │
│ 管理页面、快照、元素引用、重试、超时和错误恢复 │
└──────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────┐
│ L4 设备层：Playwright / Chromium              │
│ 实际执行导航、点击、输入、滚动、截图和等待     │
└──────────────────────────────────────────────┘
```

### 3.2 浏览器任务状态机

每一次浏览器任务必须有明确状态，不能只依赖聊天文本：

```text
CREATED
  ↓
OBSERVING
  ↓
PLANNING
  ↓
WAITING_CONFIRMATION（必要时）
  ↓
EXECUTING
  ↓
VERIFYING
  ├── 未完成 → OBSERVING
  ├── 完成   → SUCCEEDED
  ├── 可恢复错误 → RECOVERING
  └── 不可恢复错误 → FAILED
```

建议的任务状态：

- `created`：任务刚创建。
- `observing`：正在获取页面状态。
- `planning`：模型正在决定下一步工具。
- `waiting_confirmation`：等待用户确认有副作用的动作。
- `executing`：正在执行浏览器动作。
- `verifying`：正在验证动作是否生效。
- `recovering`：正在重新快照、等待或重建定位。
- `succeeded`：任务完成。
- `cancelled`：用户取消。
- `failed`：任务失败。

## 4. 浏览器工具设计

### 4.1 第一阶段工具集

保留已有工具，并补齐缺失的基础动作：

| 工具 | 作用 | 是否返回新快照 |
|---|---|---:|
| `browser_navigate` | 打开网址 | 是 |
| `browser_snapshot` | 获取当前页面结构 | 是 |
| `browser_click` | 点击元素 | 是 |
| `browser_fill` | 填写输入框 | 是 |
| `browser_press` | 按下 Enter、Tab、Escape 等按键 | 是 |
| `browser_scroll` | 滚动页面 | 是 |
| `browser_wait` | 等待页面加载或动态内容出现 | 可选 |
| `browser_screenshot` | 保存当前页面截图 | 否 |
| `browser_tabs` | 查看、切换、关闭标签页 | 否/是 |

第一阶段不开放任意 `evaluate_script`。如果以后确实需要脚本执行，也必须放进单独的高风险工具，并经过权限和确认层。

### 4.2 工具参数规范

#### `browser_navigate`

```json
{
  "url": "https://www.example.com",
  "wait_until": "domcontentloaded"
}
```

约束：

- 仅允许 `http`、`https`、必要时允许 `file`。
- 禁止默认打开 `javascript:`、`data:` 等协议。
- 对网址长度、重定向次数和加载时间设置上限。

#### `browser_snapshot`

```json
{
  "include_text": true,
  "max_elements": 120,
  "max_text_length": 12000
}
```

快照应包含：

- 当前网址和页面标题。
- 页面是否正在加载。
- 可交互元素的 `ref`。
- 元素角色、名称、可见文本、placeholder、类型。
- 必要的链接目标，但不返回 Cookie、Authorization、密码值和完整隐私数据。

#### `browser_click`

```json
{
  "ref": "e3",
  "button": "left",
  "click_count": 1
}
```

约束：

- `ref` 必须来自最近一次快照。
- 不允许模型直接传入任意 CSS 选择器作为首选定位方式。
- 如果 ref 已失效，返回结构化的 `STALE_REF`，要求重新快照。

#### `browser_fill`

```json
{
  "ref": "e2",
  "text": "莲心AI"
}
```

约束：

- 默认只允许填写 `input`、`textarea`、`contenteditable`。
- 密码框、支付信息、验证码等敏感输入必须单独标记并请求确认。
- 日志中不得记录完整输入值，只记录长度、字段类型和脱敏摘要。

#### `browser_press`

```json
{
  "key": "Enter",
  "ref": "e2"
}
```

支持 `Enter`、`Tab`、`Escape`、方向键和常用组合键，禁止任意系统级快捷键默认执行。

#### `browser_scroll`

```json
{
  "amount": 500,
  "ref": "e0"
}
```

滚动后应返回页面摘要，避免模型连续滚动而不观察结果。

## 5. 页面快照与 ref 设计

### 5.1 ref 是短期引用，不是永久 ID

网页刷新、局部更新、弹窗出现、路由切换后，`e0` 可能已经指向另一个元素。因此：

```text
旧快照中的 ref 不能跨页面状态长期复用
```

每次快照应生成一个 `snapshot_id`：

```json
{
  "snapshot_id": "snap_20260803_001",
  "page_url": "https://example.com/search",
  "refs": [
    {
      "ref": "e2",
      "role": "textbox",
      "name": "搜索",
      "tag": "input"
    }
  ]
}
```

工具调用时可以附带 `snapshot_id`：

```json
{
  "ref": "e2",
  "snapshot_id": "snap_20260803_001",
  "text": "天气"
}
```

如果当前快照已经不是该 ID，控制器应主动拒绝旧引用并返回 `STALE_SNAPSHOT`。

### 5.2 元素定位优先级

元素定位应按以下顺序：

1. 当前快照生成的 `data-lx-ref`。
2. 稳定的 `id`。
3. `name`、ARIA role、accessible name。
4. placeholder 或明确文本。
5. 受控 CSS 定位器。
6. 最后的视觉或坐标兜底。

不得把完整 CSS 路径直接暴露给模型作为主要控制接口，因为动态网页中的 CSS 层级通常不稳定。

### 5.3 快照内容限制

快照必须限制：

- 元素数量：默认不超过 120 个。
- 单个文本长度：默认不超过 500 字符。
- 总文本长度：默认不超过 12,000 字符。
- 隐藏元素：默认排除。
- 重复导航、脚本和样式文本：排除。
- Cookie、Token、Authorization、密码：脱敏或不返回。

超过限制时，返回分页或“需要缩小范围”的提示，不应把整个网页原文直接塞进 LLM 上下文。

## 6. Browser Skill 任务循环

### 6.1 标准循环

```text
1. 创建浏览器任务
2. 获取当前页面快照
3. 判断是否已经满足任务
4. 如果未完成，选择一个最小动作
5. 执行动作
6. 获取动作结果和新快照
7. 验证页面变化
8. 重复 3～7，直到完成、取消或达到上限
```

### 6.2 每轮只允许一个主要动作

不建议模型一次输出：

```text
点击 e1 → 填写 e2 → 点击 e5 → 等待 → 再点击 e8
```

推荐每轮只执行一个主要动作：

```text
快照 → 点击 e1 → 新快照
快照 → 填写 e2 → 新快照
快照 → 点击 e5 → 新快照
```

这样可以避免页面状态变化后，后续 ref 全部失效。

### 6.3 完成判断

任务不能只根据“工具调用成功”判断完成。必须满足至少一个完成条件：

- 页面出现目标文本。
- 页面 URL 进入目标地址。
- 指定元素出现或消失。
- 表单状态发生预期变化。
- 用户明确要求的结果已经返回。

如果没有可验证条件，莲心应向用户说明“动作已执行，但无法确认最终结果”，而不是假装成功。

### 6.4 循环上限

默认限制：

- 单任务最多 20 个浏览器动作。
- 单动作默认超时 15 秒。
- 单任务最长 5 分钟。
- 连续 3 次同类错误后停止自动重试。

用户明确要求复杂任务时，可以在安全范围内提高上限，但必须记录原因。

## 7. 错误处理与恢复

所有工具结果都应包含可判断的状态，而不是只返回自然语言错误。

建议统一结构：

```json
{
  "ok": false,
  "code": "STALE_REF",
  "message": "元素引用已失效，请重新获取页面快照",
  "recoverable": true,
  "suggested_action": "browser_snapshot",
  "snapshot": null
}
```

### 7.1 错误分类

| 错误码 | 含义 | 恢复方式 |
|---|---|---|
| `STALE_REF` | ref 已失效 | 重新 snapshot |
| `STALE_SNAPSHOT` | snapshot_id 不匹配 | 重新 snapshot |
| `ELEMENT_NOT_FOUND` | 页面没有目标元素 | snapshot 或询问用户 |
| `ELEMENT_NOT_VISIBLE` | 元素存在但不可见 | scroll、wait、重新 snapshot |
| `ELEMENT_NOT_EDITABLE` | 目标不是输入控件 | 重新选择输入框 |
| `PAGE_LOADING` | 页面仍在加载 | wait |
| `NAVIGATION_TIMEOUT` | 导航超时 | 重试一次，随后失败 |
| `BROWSER_NOT_READY` | 浏览器未启动 | 重建 BrowserController |
| `PERMISSION_REQUIRED` | 需要用户确认 | waiting_confirmation |
| `SENSITIVE_ACTION_BLOCKED` | 敏感动作被拦截 | 请求明确确认 |
| `TASK_LIMIT_REACHED` | 达到动作或时间上限 | 停止并汇报 |

### 7.2 重试原则

只对确定可恢复的错误重试：

1. `PAGE_LOADING`：等待后重试。
2. `STALE_REF`：重新 snapshot 后重新定位。
3. `ELEMENT_NOT_VISIBLE`：滚动或等待后重试。
4. 浏览器连接断开：重建页面，不重复提交型动作。

对于发送消息、提交订单、删除内容等动作，连接断开后不得自动重复执行，避免重复副作用。

## 8. 安全与隐私设计

### 8.1 浏览器权限分级

浏览器动作分为四级：

#### A 级：只读动作

- 打开网页
- 获取页面快照
- 滚动页面
- 截图
- 读取公开文本

默认允许。

#### B 级：低风险交互

- 普通搜索
- 展开菜单
- 切换标签页
- 填写非敏感搜索框

默认允许，但需要记录。

#### C 级：外部提交动作

- 发送评论、消息、邮件
- 提交表单
- 发布内容
- 上传文件

默认需要用户确认。

#### D 级：高风险动作

- 删除数据
- 购买商品
- 支付
- 修改账户安全设置
- 输入密码、验证码、API Key
- 下载并执行文件

默认禁止自动执行，必须明确确认，并且部分动作应直接不支持。

### 8.2 数据脱敏

以下内容不得进入普通调试日志：

- Cookie
- Session Token
- Authorization Header
- 密码
- 验证码
- API Key
- 身份证、银行卡等敏感信息

输入日志只记录：

```text
字段类型：search
长度：8
摘要：莲心AI…
```

### 8.3 浏览器 Profile 隔离

浏览器持久化目录必须继续使用莲心专用目录：

```text
~/.lianxin/browser_profile/
```

不得默认接管用户日常 Chrome Profile，避免误操作私人网页和泄露登录状态。

未来若增加“接管外部 Chrome”功能，必须：

- 由用户主动开启。
- CDP 只监听 `127.0.0.1`。
- 启动时显示当前被接管的浏览器窗口。
- 任务结束后主动断开。

## 9. 视觉和桌面级兜底方案

### 9.1 使用原则

桌面级自动化只作为浏览器 DOM 控制失败后的兜底，不作为第一方案：

```text
Playwright DOM
    ↓ 失败
浏览器截图 / ARIA 信息
    ↓ 仍失败
Windows UI Automation
    ↓ 仍失败
PyAutoGUI 坐标操作
```

### 9.2 为什么不能优先依赖坐标

坐标点击容易受到以下因素影响：

- 窗口移动和缩放。
- DPI 缩放。
- 多显示器布局。
- 浏览器最小化。
- 页面滚动位置。
- 弹窗遮挡。
- 用户同时移动鼠标。
- 远程桌面或锁屏状态。

因此，坐标操作必须带有截图、屏幕尺寸和失败风险说明。

### 9.3 Docker 限制

PyAutoGUI 和桌面 UI Automation 依赖真实显示器，不能作为无界面 Docker 服务的核心能力。Docker 版本只能默认提供：

- 浏览器后台服务。
- Playwright 无头或虚拟显示器模式。
- 页面快照和结构化操作。

完整桌面级控制保留在 Windows 桌面版。

## 10. 与 super-agent-party 的架构对照

| 能力 | super-agent-party | 莲心计划 |
|---|---|---|
| 浏览器内核 | Electron 内置 Chromium | Playwright 持久化 Chromium |
| 内部控制 | CDP + `window.aiBrowser` | BrowserController 直接调用 Playwright |
| 外部扩展 | MCP、BrowserMCP、Playwright MCP | 先复用现有 MCP 体系，后续可扩展 |
| 页面定位 | UID / WebView Snapshot | `data-lx-ref` + ARIA/DOM 信息 |
| 操作反馈 | 操作后重新获取快照 | 保持并加强操作后重新快照 |
| 桌面兜底 | PyAutoGUI + UI Automation | 后续作为独立 Computer Skill，不与浏览器 Skill 混合 |
| 风险控制 | 由工具和配置决定 | 增加明确的权限分级与用户确认 |
| 适合场景 | 自带浏览器工作台 | 莲心独立浏览器自动化与未来外设统一架构 |

## 11. 详细实施阶段

### 阶段 0：基线确认与回归测试

目标：确保升级不会破坏当前浏览器和普通聊天。

任务：

1. 确认现有浏览器 Skill 能被正确激活。
2. 确认五个现有工具都能进入 `TOOL_DEFINITIONS` 和 `TOOL_EXECUTORS`。
3. 确认浏览器工具资源锁仍然生效。
4. 测试浏览器启动、导航、快照、点击、填写、截图。
5. 测试浏览器关闭后重新启动。
6. 测试普通聊天不被浏览器工具污染。

验收：不使用浏览器时，普通聊天、拍一拍、记忆和已有工具均正常。

### 阶段 1：补齐基础浏览器工具

新增：

- `browser_press`
- `browser_scroll`
- `browser_wait`
- `browser_tabs`

同时统一工具返回格式，保留现有中文消息兼容性。

验收：能够完成“打开网页、搜索关键词、滚动、查看结果、切换标签页”的完整流程。

### 阶段 2：引入 snapshot_id 和 ref 生命周期

任务：

1. 为每次 snapshot 生成唯一 ID。
2. 工具调用校验 snapshot_id。
3. 失效 ref 返回结构化 `STALE_REF`。
4. 控制器在动作成功后生成新的快照 ID。
5. 限制旧快照在任务历史中的大小。

验收：页面刷新后，使用旧 ref 会被拒绝并自动引导重新 snapshot。

### 阶段 3：实现 Browser Skill 任务循环

任务：

1. 增加浏览器任务上下文。
2. 管理任务状态和步骤编号。
3. 每轮只允许一个主要浏览器动作。
4. 动作后自动观察和验证。
5. 增加动作数、总时长和连续失败限制。

验收：用户只说“打开网页并搜索某内容”，莲心能够连续完成任务，不需要用户反复提醒“请调用工具”。

### 阶段 4：安全确认与敏感信息保护

任务：

1. 为工具增加风险等级。
2. 对提交、发送、删除、上传、支付等动作弹出确认。
3. 对输入内容和快照进行脱敏。
4. 增加任务取消按钮。
5. 增加浏览器能力开关和会话级授权。

验收：普通搜索无需确认；发送消息、删除内容等动作无法静默执行。

### 阶段 5：调试日志和可观测性

每个浏览器任务应记录：

```json
{
  "task_id": "browser_task_xxx",
  "step": 3,
  "tool": "browser_click",
  "ref": "e4",
  "snapshot_id": "snap_xxx",
  "url": "https://example.com",
  "duration_ms": 421,
  "ok": true,
  "error_code": null,
  "redacted": true
}
```

日志必须做到：

- 能定位“没有调用工具”还是“工具调用失败”。
- 能定位页面快照是否过期。
- 能定位浏览器是否启动、导航是否完成。
- 不记录密码、Cookie 和完整敏感文本。
- 允许用户在调试界面查看最近一次任务步骤。

### 阶段 6：可选 CDP 接管模式

只有在以下需求出现后再实现：

- 需要控制用户已打开的 Chrome/Edge。
- 需要连接外部浏览器调试端口。
- 需要让多个智能体共享浏览器标签页。

实现方式优先使用 Playwright `connect_over_cdp`，而不是先复制 Electron 的完整 CDP 桥接层。

## 12. 测试方案

### 12.1 单元测试

测试：

- URL 校验。
- ref 生成和失效。
- snapshot_id 校验。
- 输入脱敏。
- 风险等级判断。
- 错误码映射。
- 任务循环上限。

### 12.2 浏览器集成测试

准备一个本地测试网页，包含：

- 搜索框。
- 普通按钮。
- 延迟加载按钮。
- 弹窗。
- 动态替换元素。
- 多标签页链接。
- 删除按钮。

验证：

1. 普通搜索可以自动完成。
2. 动态刷新后旧 ref 会失效。
3. 延迟元素会等待后再操作。
4. 删除按钮必须请求确认。
5. 页面无法加载时不会无限重试。

### 12.3 回归测试

重点保证：

- 普通聊天不受影响。
- 其他工具调用不受浏览器资源锁影响。
- 浏览器关闭后莲心仍能正常启动。
- 浏览器登录状态仍保存在用户目录。
- 日志不会泄露敏感信息。
- SQLite、记忆、成就和桌面界面功能不受影响。

## 13. 性能目标

目标值：

| 指标 | 目标 |
|---|---:|
| 浏览器工具注册耗时 | 不增加明显启动延迟 |
| 本地 snapshot 生成 | 普通页面小于 500ms |
| 点击/填写动作 | 普通页面小于 2s |
| 失效 ref 恢复 | 重新 snapshot 后 1～2 个循环内恢复 |
| 单任务上下文增长 | 只保留最近若干快照摘要 |
| 页面文本输入模型 | 默认限制 12,000 字符 |

浏览器工具属于共享状态资源，默认串行执行，不得为了追求并发而让多个动作同时操作同一个页面。

## 14. 必须避免的问题

1. **只返回“好的，我去搜索”，但没有真实工具调用。**
   - 必须在工具审计中区分“模型口头承诺”和“实际调用”。

2. **模型连续使用过期 ref。**
   - 操作后必须重新快照，ref 必须有生命周期。

3. **点击成功就认为任务完成。**
   - 必须验证页面状态或目标结果。

4. **大量网页原文直接塞进上下文。**
   - 使用快照摘要、长度限制和分页。

5. **使用固定坐标作为主要自动化方式。**
   - 坐标只能作为最后兜底。

6. **默认接管用户日常浏览器。**
   - 使用莲心专用 Profile，接管外部浏览器必须显式开启。

7. **任意 JavaScript 执行。**
   - 第一版禁用，后续也只能通过高风险权限控制。

8. **网络断开后重复提交。**
   - 对有副作用的动作必须采用“未知结果”状态并请求用户确认。

9. **浏览器工具与普通工具并行执行。**
   - 浏览器共享资源必须继续串行锁定。

10. **把桌面控制和网页控制混成一个工具。**
    - 浏览器 Skill、Computer Skill、外设 Skill 应保持边界清晰。

## 15. 第一版预期效果

完成阶段 0～4 后，莲心应能够稳定完成以下任务：

```text
打开百度并搜索“莲心AI”
打开指定网页并读取标题
在网页输入框中填写内容
点击搜索、展开、翻页等普通控件
滚动页面并继续读取结果
打开多个标签页并切换
遇到动态页面时自动重新观察
遇到发送、删除、提交动作时请求确认
```

用户看到的表现应从：

```text
“好的，我这就去搜索。”
```

变成：

```text
正在打开网页 → 已找到搜索框 → 正在输入 → 正在查看结果 → 已完成
```

同时，调试日志能够明确显示：

```text
任务创建
工具是否注册
工具是否调用
调用参数是否脱敏
页面快照内容摘要
动作执行结果
是否发生 ref 失效
最终成功或失败原因
```

## 16. 最终架构原则

莲心浏览器自动化必须遵循以下原则：

```text
LLM 决定意图，不直接操控设备
Skill 管理任务，不承担底层定位细节
Controller 管理状态，不让前端成为权威状态
Playwright 执行网页动作，不依赖固定坐标
每次动作之后重新观察，不凭记忆猜测页面
高风险操作必须确认，敏感信息必须脱敏
浏览器能力可独立测试，也可未来接入 MCP/CDP
```

这套结构与莲心正在建设的“Intent → Skill → Controller → Device”架构一致。未来如果把 Chromium 替换成真实 Chrome、Electron WebView、远程浏览器或 Docker 中的浏览器服务，上层 Browser Skill 和任务状态机都不需要重写。

## 17. 阶段 1 详细设计：基础浏览器动作扩展

阶段 1 的目标不是实现完整自主浏览器智能体，而是把现有的“导航、快照、点击、填写、截图”扩展为可完成基本网页任务的最小动作集合。

### 17.1 阶段 1 的范围

本阶段新增四类工具：

```text
browser_press   按键
browser_scroll  页面滚动
browser_wait    受控等待
browser_tabs    标签页管理
```

本阶段明确不做：

- 任意 JavaScript 执行。
- 屏幕坐标点击。
- 自动处理验证码。
- 自动输入密码、支付信息和 API Key。
- CDP 接管外部 Chrome。
- 多个浏览器任务同时操作同一页面。

### 17.2 `browser_press`

#### 设计目的

补齐 Enter 提交搜索、Tab 切换焦点、Escape 关闭弹窗、方向键选择等常见交互。

#### 工具参数

```json
{
  "key": "Enter",
  "ref": "e2"
}
```

参数规则：

- `key` 必填。
- `ref` 可选；填写后先对指定元素聚焦，再发送按键。
- 允许的按键包括：`Enter`、`Tab`、`Escape`、`Space`、方向键、`Home`、`End`、`PageUp`、`PageDown`。
- 组合键采用受控格式，例如 `Control+L`、`Control+A`、`Shift+Tab`。
- 默认禁止 `Alt+F4`、`Control+W`、`Control+Shift+Delete` 等可能关闭窗口或清理数据的快捷键。

#### 执行流程

```text
校验 key
    ↓
校验 ref 是否属于当前快照
    ↓
聚焦元素（如果提供 ref）
    ↓
page.keyboard.press(key)
    ↓
等待短暂稳定时间
    ↓
返回新快照
```

### 17.3 `browser_scroll`

#### 设计目的

让模型可以阅读长页面、找到被滚动区域遮挡的元素，而不是依赖坐标操作。

#### 工具参数

```json
{
  "amount": 500,
  "ref": "e0"
}
```

规则：

- `amount` 为整数，正数向下，负数向上。
- 单次最大滚动量默认限制为 2,000 像素。
- `ref` 可选；如果提供，优先滚动该元素所在容器，否则滚动页面。
- 执行后返回当前位置摘要、页面高度和新的可交互元素快照。
- 不允许模型通过 amount 生成无限滚动循环；任务循环仍受总步骤限制。

推荐返回信息：

```text
滚动完成：向下 500px
页面位置：500 / 4280
页面快照：……
```

### 17.4 `browser_wait`

#### 设计目的

处理动态加载、跳转、弹窗和异步搜索结果，避免模型在页面未稳定时盲目点击。

#### 工具参数

```json
{
  "seconds": 1.5,
  "until": "network_idle"
}
```

`until` 只允许以下有限条件：

- `time`：等待指定秒数。
- `domcontentloaded`：等待页面 DOM 加载完成。
- `network_idle`：等待网络空闲，但设置最大超时。
- `text`：等待指定文本出现，另传 `text` 参数。
- `ref_visible`：等待指定 ref 对应元素出现，另传 `ref` 参数。
- `url_contains`：等待 URL 包含指定片段，另传 `value` 参数。

禁止传入任意 JavaScript 条件表达式，避免形成隐蔽的脚本执行入口。

#### 超时策略

- 默认最多等待 10 秒。
- 单次不得超过 30 秒。
- `network_idle` 不能无限等待，超时后返回“页面可能仍在加载”的可恢复结果。
- 等待完成后是否返回快照由 `refresh_snapshot` 参数控制，默认开启。

### 17.5 `browser_tabs`

#### 设计目的

支持打开链接后切换标签页、查看当前页面、关闭无用标签页，避免多个页面状态混在一处。

#### 工具参数

```json
{
  "action": "list",
  "page_id": "p1",
  "url": "https://example.com"
}
```

支持的动作：

| action | 作用 | 参数 |
|---|---|---|
| `list` | 列出标签页 | 无 |
| `select` | 切换标签页 | `page_id` |
| `new` | 新建标签页并可选导航 | `url` 可选 |
| `close` | 关闭标签页 | `page_id` |

标签页使用莲心自己的短期 ID：

```json
[
  {"page_id":"p0", "active":false, "title":"百度", "url":"https://www.baidu.com"},
  {"page_id":"p1", "active":true, "title":"搜索结果", "url":"https://..."}
]
```

安全规则：

- 不返回 Cookie、Profile 路径和调试端口。
- 关闭当前唯一标签页前必须新建空白页或拒绝操作。
- 关闭含未提交表单的页面时，必须请求用户确认。
- `select` 后立即返回该页面快照，避免模型仍使用旧页面的 ref。

### 17.6 BrowserController 内部扩展

阶段 1 只扩展现有 `BrowserController`，不新建第二套浏览器实例。

建议新增方法：

```python
press(key: str, ref: str | None = None) -> str
scroll(amount: int = 300, ref: str | None = None) -> str
wait(seconds: float = 1.0, until: str = "time", ...) -> str
list_tabs() -> str
select_tab(page_id: str) -> str
new_tab(url: str | None = None) -> str
close_tab(page_id: str) -> str
```

内部状态建议扩展为：

```python
self._page_ids: dict[str, Page]
self._active_page_id: str | None
self._last_refs: list[dict]
```

其中真实 Playwright `Page` 对象只保存在控制器内部，工具和模型只看到 `p0`、`p1` 等不透明 ID。

### 17.7 兼容性返回协议

现有浏览器工具返回字符串，阶段 1 不直接改变外部调用格式，避免破坏当前 Agent 和聊天界面。

内部可先统一为：

```python
BrowserActionResult(
    ok=True,
    action="browser_click",
    code="OK",
    page_id="p0",
    url="https://example.com",
    title="示例",
    snapshot="...",
    recoverable=False,
)
```

在 Skill 工具边界再格式化为模型可读文本，并保留稳定的状态前缀：

```text
[BROWSER_OK] action=browser_click page=p0
...
```

失败时：

```text
[BROWSER_ERROR] code=STALE_REF recoverable=true next=browser_snapshot
元素引用已失效，请先刷新页面快照。
```

这样既兼容现有字符串工具，又为阶段 2 的结构化 `snapshot_id` 和调试日志预留接口。

### 17.8 路由、权限和资源锁同步修改

新增工具必须同步进入以下位置，不能只修改技能文件：

1. `skills/浏览器自动化/tools.py`：定义和执行器。
2. `skills/浏览器自动化/SKILL.md`：能力说明和使用流程。
3. `brain/request_router.py`：`browser` 能力对应的工具集合。
4. `brain/request_tool_policy.py`：浏览器 URL 请求白名单。
5. `brain/agent.py`：`browser` 资源组和线程亲和约束。
6. `brain/tool_registry.py`：工具统计分类。
7. `skills/_提示词指南/browser.md`：模型操作指南。
8. `tests/test_browser_automation_baseline.py`：工具契约和路由测试。

浏览器工具必须全部归入：

```text
RESOURCE_GROUP = browser
THREAD_AFFINE = true
```

同一任务内的 `browser_wait`、`browser_tabs`、`browser_click` 也不能与其他浏览器动作并行执行。

### 17.9 阶段 1 测试设计

阶段 1 仍然不依赖公共互联网，使用本地 HTML 夹具：

```text
tests/fixtures/browser_basic.html
```

页面至少包含：

- 搜索输入框。
- Enter 提交按钮。
- 动态延迟显示区域。
- 可滚动长列表。
- 打开新标签页链接。
- 可关闭弹窗。
- 一个必须确认的删除按钮。

测试项目：

1. `browser_press(Enter)` 能提交输入框。
2. `browser_press(Tab)` 不会造成异常快捷键行为。
3. `browser_scroll` 限制最大滚动量。
4. `browser_wait` 支持时间、文本、URL 条件。
5. 等待超时不会无限阻塞。
6. `browser_tabs list/select/new/close` 状态正确。
7. 切换标签页后旧页面 ref 不会继续误用。
8. 浏览器工具仍然保持线程亲和资源锁。
9. 普通网页搜索仍优先使用 `web_search`，不会因新增工具而启动浏览器。
10. 真实浏览器未安装时，工具返回可读错误而不是让 Agent 崩溃。

### 17.10 阶段 1 验收标准

阶段 1 完成后，莲心至少应能完成：

```text
打开本地测试页面
填写搜索框
按 Enter 提交
等待异步结果
滚动到页面下方
打开第二个标签页
切换并读取第二页
返回原页面继续操作
```

同时满足：

- 普通聊天不加载浏览器动作工具。
- 普通联网搜索不强制启动浏览器。
- 浏览器动作均串行执行。
- 浏览器关闭后下一次操作可以恢复实例。
- 阶段 0 的 15 项回归测试继续通过。
- 不新增 CDP、PyAutoGUI 或任意脚本执行依赖。

## 18. 阶段 7：网页研究与浏览器交互复合 Skill

为区分“搜索网页”“读取网页”和“搜索后交给浏览器继续操作”，莲心新增
`skills/网页研究与浏览器交互/`。它是编排技能，不新增重复的搜索或点击
接口，执行层由 `brain/web_research_task.py` 维护本地状态机：

```text
用户：搜索并打开/阅读/操作
        ↓
web_search（只负责获取候选来源）
        ↓ 选择原始 URL
fetch_webpage 或 browser_navigate / browser_connect
        ↓
原有浏览器快照、点击、填写与确认流程
```

边界规则：

- 只说“搜索/查资料”时，仍只调用 `web_search`，不会自动打开浏览器。
- 只给 URL 并要求读取时，仍优先 `fetch_webpage`，不会平白增加搜索调用。
- 明确说“接管已经打开的浏览器”时，搜索完成后先调用 `browser_connect`，
  且 CDP 仍只允许本机 loopback 地址。
- 复合任务在搜索证据返回前不能跳过搜索，搜索后不能用一句“我去打开”
  冒充已完成；最终回复继续以工具审计记录为准。

该阶段的契约测试位于 `tests/test_web_research_task.py`，并与浏览器自动化
回归测试一起验证。这样未来增加“搜索后登录并填写表单”等 Skill 时，可以
继续复用同一个意图路由、状态机与浏览器安全边界。
