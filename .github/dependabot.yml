version: 2
updates:
  # Python 依赖每周检查一次，合并前仍由质量工作流验证。
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # GitHub Actions 固定使用受维护的动作版本。
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
