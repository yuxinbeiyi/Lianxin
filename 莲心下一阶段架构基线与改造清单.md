# 莲心下一阶段架构基线与改造清单

> 审计日期：2026-07-26
> 本轮范围：记忆、文档阅读、涟漪情感、主动行为、自动任务
> 原则：复用现有 SQLite、AgentCore 和后台职责中心，不引入第二套平行架构。

## 1. 结论

莲心当前最需要的不是继续增加新功能，而是把已经存在的能力整理成稳定、可追踪、低重复调用的流水线。

下一阶段的最高收益工作是：

1. 将自动记忆提取从 `AgentCore` 内部线程迁移为独立后台职责；
2. 为记忆提取增加持久游标、运行记录、失败恢复和模型成本统计；
3. 提高新记忆的原始消息来源覆盖率；
4. 复用现有事实、碎片、实体、Episode、Saga、维护和混合检索能力；
5. 暂不重写涟漪情感系统；它已经具备连续动力学、边界约束、技术话题抑制、审计和主动策略测试；
6. MarkItDown 转换缓存继续保留，但“统一模型请求前文档预处理”应放在记忆流水线稳定后的任务工作流阶段完成。

## 2. 当前真实架构

### 2.1 记忆链路

```text
用户消息
  ↓
HistoryManager 写入 sessions / messages
  ↓
AgentCore 正常回复
  ↓
每达到 extract_interval 轮触发后台提取
  ├─ 分类事实提取 → memory_facts + memory_fragments
  ├─ 相似事实候选 → 冲突模型裁决
  └─ 五元组提取 → graph_entities + graph_edges
  ↓
后台职责中心
  ├─ 工作记忆摘要：按需检查，当前配置 10 分钟
  ├─ 记忆维护：当前配置 6 小时
  └─ 叙事整理：当前配置 12 小时
       └─ entity_profiles → episodes → sagas
  ↓
memory_rag
  └─ 向量 + FTS/LIKE + Episode + Entity 的 RRF 混合检索
```

当前配置：

| 配置 | 当前值 |
|---|---:|
| 自动记忆提取 | 开启 |
| 提取间隔 | 20 轮 |
| 单次消息窗口 | 15 条 |
| 自动五元组提取 | 开启 |
| 工作记忆模型摘要 | 10 分钟检查 |
| 记忆维护 | 6 小时 |
| 叙事整理 | 12 小时 |
| 叙事候选批量 | 36 条 |

### 2.2 文档阅读链路

```text
模型调用 read_file / QQ、微信收到文件
  ↓
_extract_full_text
  ├─ DOCX/PDF → MarkItDown
  ├─ 转换失败 → 旧版提取器降级
  └─ XLSX/文本 → 对应提取器
  ↓
MarkdownDocumentCache
  ├─ SHA-256 内容寻址
  ├─ 相同内容只转换一次
  ├─ 原子写入
  ├─ 30 天过期
  └─ 最大 128 MiB，按最近使用淘汰
  ↓
按 15000 字符分块返回模型
```

当前缓存位于 `%USERPROFILE%\.lianxin\document_cache`。审计时存在 4 个缓存文件，总计 9965 字节。

重要边界：当前 MarkItDown 已避免重复转换和污染原目录，但它仍主要在 `read_file` 或桥接层收到文件时触发，并不是所有 LLM 请求前都会自动扫描、批量转换文档。

### 2.3 涟漪情感和主动行为链路

```text
当前用户消息
  ↓
EmotionManager.prepare_turn
  ↓
Appraisal 事件评估
  ↓
EmotionalDynamics 连续状态推进
  ↓
EmotionStore 原子保存状态和事件
  ↓
语调提示注入本轮模型上下文
  ↓
回复完成后记录协作结果
  ↓
DutyScheduler 根据情感动机、安静时段、冷却和用户状态决定主动行为
```

现有测试已经覆盖：

- 状态和 delta 边界；
- 长时间推进与短步推进一致性；
- 技术讨论不被误判为关系伤害；
- 冲突、道歉、修复和防御；
- 人格间情感状态隔离；
- 事件幂等和并发写入；
- Saga 对情感只产生小幅偏置；
- 主动联系遵守用户延后、冷却和安静时段。

结论：涟漪情感系统目前应以补充场景模拟和轨迹导出为主，不适合近期重构核心模型。

### 2.4 自动任务链路

```text
自然语言任务
  ↓
AutoTaskParser 提取调度信息
  ↓
AutoTaskManager 保存 auto_tasks.json
  ↓
AutoTaskScheduler 每 30 秒检查到期/错过任务
  ↓
AutoTaskExecutor 运行 ReAct 循环
  ↓
按需选择工具、重试网络错误、检测死循环、支持取消
  ↓
auto_task_logs.json 保存最近执行记录
```

当前能力包括一次性、间隔、每日、每周、每月任务，以及暂停、恢复、取消、错过任务处理和执行日志。

当前不足：

- 任务和日志仍是 JSON 文件；
- 没有统一的 Workflow/Step 数据模型；
- 缺少自动任务专项测试；
- GUI 进度条只表达总体进度，不等于可恢复的步骤状态；
- 审计时当前任务数为 0，历史日志为 29 条。

## 3. 当前数据库基线

主数据库：`memory/conversations.db`

| 数据 | 数量 |
|---|---:|
| 会话 | 224 |
| 消息 | 10723 |
| 长期事实 | 209 |
| 事实碎片 | 121 |
| 实体画像 | 14 |
| Episodes | 24 |
| Sagas | 3 |
| 当前状态 | 8 |
| 工作记忆话题 | 142 |
| 维护运行记录 | 15 |
| 叙事运行记录 | 12 |
| 叙事审计事件 | 2463 |
| 情感 v3 事件 | 141 |

运行状态：

- 15 次记忆维护全部成功；最近一次耗时约 690 ms；
- 12 次叙事整理全部成功；最近一次处理 11 个候选，创建 2 个 Episode，更新 9 个实体；
- 209 条事实全部为 active；206 条处于 maturing，3 条 stable；
- 24 个 Episode 中 7 个 active、15 个 merged、2 个 needs_review；
- 3 个 Saga 全部 active。

## 4. 已经做得好的部分

以下能力应直接复用：

1. `memory_facts` 与 `memory_fragments` 的事实/证据分层；
2. 新记忆的指纹幂等；
3. 相似度只生成冲突候选，不直接破坏旧记忆；
4. 纠错、冲突裁决和关联叙事的审计；
5. 当前状态自动过期；
6. 工作记忆自动归档和过期；
7. Entity、Episode、Saga 的叙事整理；
8. 事实质量分、访问次数、情感权重和生命周期阶段；
9. FTS/LIKE、向量、Episode、Entity 的混合检索；
10. 记忆星图和原始消息查看入口；
11. MarkItDown 内容寻址缓存和安全清理；
12. 情感系统的连续动力学、审计和主动策略。

## 5. 主要问题和风险

### P0：记忆提取没有持久处理游标

`AgentCore` 使用进程内的 `_last_extraction_idx` 和 `_extraction_inflight`。它们不能表达：

- 某个会话最后成功处理到哪条持久化消息；
- 程序重启前是否有提取任务未完成；
- 一批消息是否已经失败并等待重试；
- 同一会话在桌面、QQ、微信实例之间是否重复处理。

虽然指纹能降低重复写入，但不能避免重复模型调用。

### P0：历史记忆来源覆盖率较低

审计结果：

- 121 条 fragment 中，117 条没有原始消息 ID；
- 121 条 fragment 中，110 条没有来源 session；
- 新代码和测试已经要求精确来源，因此这主要是历史数据遗留问题。

不能凭推测为旧记忆补造消息来源。旧数据应标记为“历史来源不可验证”，新数据必须达到完整来源覆盖。

### P0：模型调用分散，缺少统一成本记录

自动记忆提取可能包含：

- 分类事实提取；
- 冲突候选裁决；
- 五元组提取；
- Checklist 提取；
- 工作记忆摘要；
- 叙事整理。

这些调用位于不同模块，当前运行记录没有统一保存输入消息范围、模型、token、耗时和失败原因。

### P1：叙事关系使用 JSON ID 数组

`memory_episodes.fragment_ids/entity_ids` 和 `memory_sagas.episode_ids/entity_ids` 使用 JSON 文本保存关系。目前没有以下关系表：

- `memory_episode_fragments`；
- `memory_episode_entities`；
- `memory_saga_episodes`。

现有实现可用，但数据库不能使用外键保证关系完整，查询、纠错传播和来源追溯需要反复解析 JSON。

### P1：生命周期主要作用于事实层

事实层已有 maturing、stable、decaying、retired 和访问刷新，但 fragment、Episode、Saga 尚未形成统一生命周期。近期不应自动清空历史内容，应先做非破坏性的热/温/冷分层和归档。

### P1：文档转换尚未进入统一请求预处理

当前文档缓存已经安全高效，但模型仍需要先决定调用 `read_file`。下一阶段任务工作流应支持：

- 用户明确附带或点名文档时，在模型首次规划前准备文档索引；
- 多文档只转换一次；
- 只注入目录、摘要和相关分块，而不是整份 Markdown；
- 不扫描用户没有放入当前任务范围的文件。

### P1：自动任务缺少专项回归测试

自动任务具备较完整的运行代码，但没有对应测试文件。任务持久化、重启恢复、取消、重复到期和日志截断都需要测试保护。

### 当前基线测试问题

本轮运行 80 项相关测试：76 项通过，4 项报错。

- 2 项因为当前命令环境没有可用的 `PyQt5.QtCore`，属于测试环境依赖问题；
- 2 项 `AgentEmotionTrackingTests` 因测试构造的 `AgentCore` 缺少 `_cancel_event` 报错，属于需要修复的测试夹具/兼容性回归。

阶段1动工前应先恢复这组基线测试的可运行状态。

## 6. 阶段1目标架构

```text
HistoryManager 保存消息
  ↓
标记会话有未处理消息（不调用模型）
  ↓
MemoryExtractionDuty
  ├─ 用户空闲达到阈值
  ├─ 未处理消息达到阈值
  └─ 手动触发
  ↓
MemoryExtractionWorker
  ├─ 从持久游标读取精确消息范围
  ├─ 一次结构化调用提取事实、实体候选和关系候选
  ├─ 写 facts / fragments
  ├─ 只有出现冲突候选时才调用裁决模型
  └─ 成功后原子推进游标
  ↓
现有 MaintenanceDuty
  ↓
现有 NarrativeDuty
  ↓
现有 Hybrid RAG
```

## 7. 阶段1最小数据库变更

### 必须新增

#### `memory_extraction_state`

每个会话只保留一条处理状态：

- `session_id`；
- `last_processed_message_id`；
- `last_success_at`；
- `last_attempt_at`；
- `consecutive_failures`；
- `updated_at`。

#### `memory_extraction_runs`

记录每次实际模型提取：

- `session_id`；
- `from_message_id` / `to_message_id`；
- `trigger`；
- `status`；
- `model`；
- `message_count`；
- `facts_created`；
- `fragments_created`；
- `conflicts_created`；
- `input_tokens` / `output_tokens`；
- `duration_ms`；
- `error`；
- `started_at` / `finished_at`。

### 建议新增

为 `memory_fragments` 增加 `provenance_status`：

- `verified`：有经过校验的原始用户消息；
- `legacy_untraceable`：历史数据无法可靠追溯；
- `invalid`：来源消息已经不存在，需要维护处理。

### 暂缓新增

Episode/Entity/Saga 关系表放在阶段1第二小步。先稳定提取游标和来源，再迁移 JSON 关系，避免一次改动过大。

## 8. 阶段1代码改造边界

### 新增

- `brain/memory_pipeline.py`：持久游标、待处理消息选择、运行记录；
- `workers/memory_extraction_worker.py`：模型提取和原子提交；
- `MemoryExtractionDuty`：空闲、积压和手动触发；
- 对应测试文件。

### 修改

- `brain/agent.py`：不再直接启动记忆模型线程，只通知流水线有新消息；
- `utils/duty_scheduler.py`：注册记忆提取职责；
- `brain/graph_memory.py`：提供事务化写入和来源状态支持；
- 记忆设置界面：增加空闲阈值、积压阈值和提取模型配置；
- 后台职责中心：显示最近提取范围、耗时、token 和失败原因。

### 保持不变

- 原始会话与消息表；
- 长期事实和 fragment 主表；
- 现有冲突与纠错逻辑；
- 当前状态与工作记忆；
- 维护和叙事 worker；
- 记忆星图；
- 涟漪情感系统；
- MarkItDown 缓存。

## 9. 阶段1模型调用策略

1. 普通每轮对话不增加额外同步调用；
2. 提取只在空闲、积压或手动触发时运行；
3. 优先使用可配置的低成本结构化输出模型；
4. 分类事实、实体和关系候选尽量由一次 JSON 输出完成；
5. 没有冲突候选时不调用冲突裁决模型；
6. Saga 继续由低频 NarrativeDuty 处理；
7. 每次调用记录模型、token、消息范围和耗时；
8. 失败不推进游标，下次从相同持久消息范围重试；
9. 连续失败达到阈值后暂停该会话并在后台职责中心提示。

## 10. 阶段1验收标准

- 程序重启后不会重新调用模型处理已成功提取的消息；
- 同一会话同一消息范围最多存在一个 running 任务；
- 新生成 fragment 的来源覆盖率达到 100%，或明确标记为无可验证来源并不自动提升为高置信度；
- 没有待处理消息时不调用提取模型；
- 冲突模型仅在确有候选时调用；
- 失败任务不丢消息、不推进游标，可安全重试；
- 所有现有记忆、叙事、纠错、隐私和情感测试继续通过；
- 新增持久游标、重启恢复、并发幂等、失败重试和 token 统计测试；
- 不修改或删除任何历史聊天和原始文档。

## 11. 后续阶段顺序

1. 阶段1：记忆提取流水线、来源覆盖和持久游标；
2. 阶段1.2：Episode/Entity/Saga 关系规范化和非破坏性生命周期；
3. 阶段2：统一 Workflow、文档预处理、搜索/浏览器缓存和步骤可视化；
4. 阶段3：情感轨迹导出和批量场景模拟；
5. 阶段4：任务中心 SQLite 化与专项测试；
6. 阶段5：角色动作、窗口形态和跨平台体验。

## 12. 阶段1第一小步实施记录（2026-07-26）

本轮已经完成：

- 修复测试基线中的 Qt 命令行环境依赖和 AgentCore 轻量夹具缺失问题；
- 新增 `memory_extraction_state`，持久保存会话提取游标、运行租约、连续失败次数和最近成功/尝试时间；
- 新增 `memory_extraction_runs`，记录消息范围、触发来源、尝试次数、模型、token、耗时、写入数量和错误；
- 新增 `brain/memory_extraction_pipeline.py`，实现原子领取、租约过期回收、成功推进游标、失败保留游标和来源校验；
- 新增 `workers/memory_extraction_worker.py`，将模型提取移出 AgentCore 的回复线程；
- AgentCore 的自动触发已经切到新流水线，旧提取函数暂时保留为回滚路径，但不再由正常对话调用；
- 旧会话首次升级只建立当前消息位置的基线，避免重新批量提取全部历史；已有持久状态的会话重启后继续处理未完成增量；
- 用户明确禁止记忆写入的轮次会推进排除游标，并留下 `policy_skip` 运行记录；
- 新增重启恢复、过期租约回收、并发幂等、失败重试、来源追溯和 token 记录测试。

下一小步重点：

- 将新流水线从 AgentCore 的间隔触发进一步迁入 `DutyScheduler`，支持空闲、积压和手动触发；
- 为连续失败增加退避与暂停阈值，并在后台职责中心展示最近运行状态；
- 在稳定运行后删除 AgentCore 中保留的旧提取实现；
- 补充真实模型契约测试，并评估把分类事实与五元组提取合并为单次结构化调用。

## 13. 阶段1第二小步实施记录（2026-07-26）

本轮已经完成：

- 新增 `MemoryExtractionDuty` 并注册到统一 `DutyScheduler`；
- AgentCore 不再按对话轮次启动提取线程，旧版自动提取函数和第一小步的过渡触发函数均已删除；
- 自动提取现在直接扫描已初始化且属于 owner 的持久会话，因此桌面、QQ 等渠道都可进入同一提取队列；
- 支持两类自动触发：会话达到空闲时间，或持久消息积压达到阈值并经过短暂安静期；
- 后台职责中心支持手动强制触发，能够绕过退避或自动暂停，对当前会话执行恢复验证；
- 连续失败采用指数退避；达到配置阈值后写入 `paused_until` 和 `pause_reason`，暂时停止自动模型调用；
- 成功运行会清除连续失败、退避和暂停状态；
- 后台职责中心新增记忆提取卡片，可展示待处理数量、运行状态、退避时间、暂停截止时间、最近消息范围、Token、耗时和错误；
- 记忆设置界面已从“每 N 轮对话”调整为空闲秒数、积压阈值、首次重试和连续失败暂停阈值；
- `memory_extraction_state` 采用原位幂等迁移增加 `next_retry_at`、`paused_until` 和 `pause_reason`，不会重建或清空旧表；
- 修复 DutyScheduler 中动态 Agent 引用和职责暂停状态会被下一次 tick 覆盖的问题；
- 新增调度触发、退避暂停、手动恢复和旧表迁移测试。

下一小步重点：

- 为记忆提取运行记录增加可展开的历史明细和单次来源查看；
- 评估将分类事实与五元组提取合并为一次结构化模型调用，进一步降低 Token 和失败面；
- 增加真实模型契约测试与小规模运行观测，校准默认空闲、积压和退避参数；
- 开始阶段1.2：Episode、Entity、Saga 关系规范化与非破坏性生命周期。

## 14. 阶段1收口与阶段1.2实施记录（2026-07-26）

本轮已经完成：

- 后台职责中心的记忆提取卡片新增“历史”入口，支持查看最近 200 次运行、完整错误、消息范围、触发原因、模型、Token、耗时、契约版本和图提取降级状态；
- 运行详情可打开本次处理的原始持久消息；失败记录可恢复该会话退避状态并提交带原运行 ID 的手动重试，重试仍从未推进的持久游标开始；
- `memory_extraction_runs` 幂等增加 `contract_version`、`graph_fallback_used`、`retry_of_run_id`，旧数据库不重建、不清空；
- 自动记忆提取升级为 `memory_extraction_v2` 契约，一次模型输出同时返回分类事实和五元组；正常路径减少一次模型调用，只有五元组字段缺失或不合法时才调用旧提取器降级；
- 主调用与降级调用的 Token 会合并计入同一运行，图边同步保存来源会话和渠道；
- 新增契约校验、单调用保证、降级路径、Token 计数和来源范围测试；这些测试使用与 LiteLLM 生产响应一致的对象契约，但不会调用真实外部 API 或消耗用户额度；
- 新增规范化 `memory_narrative_relations` 和 `memory_narrative_sources`，统一表示 Entity、Episode、Saga 之间的包含、提及、关联关系及其事实、碎片来源；
- 旧 `source_fact_ids`、`fragment_ids`、`entity_ids`、`episode_ids` 和 `related_entity_ids` JSON 字段继续保留，迁移仅做幂等镜像，便于回滚和对照；
- 新增叙事生命周期字段和 `archive / restore / needs_review / supersede` 接口；所有操作只改变派生记录和链接状态，不删除事实、碎片、原始消息或历史关系；
- 新增关系查询和来源追溯接口，可从 Saga 经过 Episode、Fragment 一直定位到原始消息；归档记录仍可通过 `include_inactive` 审计来源；
- Narrative 写入已经同步维护规范化关系，记忆宇宙的来源查看和复核标记也已接入统一接口；
- 删除 AgentCore 中最后一个记忆来源兼容包装、旧 `extract_interval` 默认配置以及未使用的旧五元组入口；独立五元组提取器仅保留为 v2 契约的显式降级路径；
- 修复 `memory_proactive` 测试数据库切换时的 schema ready 泄漏，避免不同数据库连接误判表已初始化。

验证结果：

- 新增及相关核心测试 20 项全部通过；
- 全量发现共运行 187 项，代码逻辑失败已修复；当前环境仍有 11 个测试模块因缺少 PyQt5、websocket-client、edge-tts 等运行依赖而无法导入，属于环境依赖而非本轮代码断言失败；
- GUI 文件、提取流水线、叙事模型和图存储均通过 Python 语法编译。

下一阶段建议：

- 进入阶段2，建立统一 Workflow 运行表和步骤可视化；
- 把文档预处理、搜索、浏览器等耗时工具纳入统一缓存和运行审计；
- 在安装完整桌面运行依赖的环境中补跑 Qt 界面回归，并进行一次小规模真实模型观测以校准 v2 契约服从率。

## 15. 阶段2统一 Workflow 实施记录（2026-07-26）

本轮已经完成：

- 新增独立 `%USERPROFILE%\.lianxin\workflows.db`，建立 `workflow_runs`、`workflow_steps`、`workflow_artifacts` 和 `workflow_cache`；
- 对话请求、自动任务和 DutyScheduler 后台职责均会创建统一运行记录；
- Agent 模型轮次、请求前文档预处理和所有 Function Calling 工具调用进入统一步骤审计；
- 步骤记录输入、状态、缓存命中、耗时、输出预览和完整错误，文件类工具自动登记产物；
- 用户消息明确引用本地 DOCX、PDF、Excel、TXT、Markdown、CSV 或 JSON 时，会在第一次模型调用前批量准备 Markdown；
- 文档预处理复用现有 SHA-256 内容寻址缓存，最多处理 8 份文档、注入 48,000 字符；失败时继续允许 `read_file` 降级；
- 联网搜索、普通/API 网页读取和浏览器网页读取接入带 TTL 的 Workflow 缓存；缓存最多保留 1000 条，只清理过期/LRU 内容；
- 程序重启后未完成运行和步骤会幂等迁移为 `interrupted`，不删除输入、步骤或产物；
- 新增任务运行中心，可查看最近运行、步骤、缓存、Token、错误和产物，并支持取消、对话重试和自动任务重试；
- 功能中心新增“任务运行中心”入口；重试会创建新运行并通过 `retry_of_run_id` 保留来源；
- 新增 Workflow 持久化、重启恢复、并发幂等、缓存过期、工具缓存和文档无损预处理测试；
- 详细设计见 `docs/统一工作流系统.md`。

阶段2完成后保留的边界：

- `TaskTracker` 继续承担当前会话的轻量进度条，不重复实现持久 Workflow；
- 现有 TodoManager 和 AutoTaskManager 的业务数据暂不迁移，阶段4再统一 SQLite 化；
- 浏览器导航、点击、填写等有状态操作不缓存，只缓存可安全复用的网页读取结果；
- Workflow 不删除原始文档、聊天、任务或运行审计。

下一阶段：

- 合并推进阶段3与阶段4：情感轨迹导出、批量场景模拟、Todo/AutoTask SQLite 化以及与 Workflow 的正式关系表。

## 16. 阶段3与阶段4合并实施记录（2026-07-26）

本轮已经完成：

- 新增 `brain/task_store.py`，以 `%USERPROFILE%/.lianxin/tasks.db` 统一保存 Todo、AutoTask、执行日志、任务关系与 Workflow 关系；
- 首次启动事务化迁移 `todo.json`、`auto_tasks.json` 和 `auto_task_logs.json`，迁移幂等且不修改或删除原 JSON；
- `TodoManager` 和 `AutoTaskManager` 已切换到 SQLite，同时保持原有公开接口、观察者和界面调用方式；
- Todo 的创建、更新、完成、恢复和删除进入轻量 Workflow 审计；AutoTask 每次实际执行与对应 Workflow 正式绑定；
- 自动任务日志界面可查看关联 Workflow ID，任务运行中心详情可反查关联任务实体；
- 情感存储新增 `emotion_v3_scenario_runs`，保存批量场景实验的基线、场景序列、结果、是否应用及 Workflow ID；
- 情感管理器新增非破坏性批量预演、同基线多场景对比和显式持久应用路径；
- 情感调试面板新增三组典型场景对比，默认不修改真实情感状态；
- 新增 JSON/CSV 情感轨迹导出，默认排除模拟事件，并将导出文件登记为 Workflow 产物；
- 新增旧数据迁移幂等、原文件保护、SQLite 重载、任务关系、Workflow 关系、场景非破坏性和导出契约测试；
- 详细设计见 `docs/阶段3与阶段4：情感实验室和统一任务中心.md`。

阶段3与阶段4完成后保留的边界：

- 旧任务 JSON 暂不自动删除，待用户完成真实数据验收后再人工归档；
- Workflow 和任务数据库保持逻辑关联，避免跨数据库外键导致备份与恢复耦合；
- 场景实验默认是开发/调试能力，不自动影响莲心对用户的真实关系状态；
- 下一阶段进入阶段5：角色动作、窗口形态、桌面交互和跨平台体验。

## 17. 阶段5与最终收口实施记录（2026-07-26）

本轮已经完成：

- 新增 `AvatarActionRouter`，将思考、说话、倾听、开心、庆祝、担忧、亲昵、招手和错误等产品语义统一映射到 GIF 角色与 Galgame 表情；动作带优先级、超时恢复、来源记录和过期回调保护。
- 对话回复、TTS、主动聊天、头像互动、自习室专注完成、自动化任务成功与失败均接入统一动作路由；“减少动效”模式保留状态反馈但避免高频动画。
- 新增标准窗口、紧凑聊天、桌面陪伴三种窗口形态；窗口位置、尺寸、最大化状态、始终置顶和上次模式可持久恢复，并对屏幕可用区域进行边界校正。
- 新增系统托盘菜单、关闭到托盘、最小化到托盘和桌面通知；后台任务与主动聊天在主窗口隐藏时仍能给出可控提醒。
- Windows 保留原生全局热键、任务栏提醒和命名互斥量；其他平台降级为窗口内快捷键和文件锁，开机自启动等平台专属能力在不支持时显式禁用。
- 修复非 Windows 环境导入 `ctypes.windll` 和线程模块的阻断问题，并补齐单实例句柄释放，避免重复实例检测留下悬挂互斥量。
- 启动体检新增“运行底座”，启动时验证平台能力、初始化 `tasks.db` 与 `workflows.db` 的幂等迁移，并恢复中断的 Workflow 运行。
- 新增阶段5纯逻辑测试，覆盖平台能力、单实例获取与释放、动作优先级、过期定时器、减少动效和来源追踪。

最终收口边界：

- 当前角色资源仍复用已有 GIF 与 Galgame 表情，不引入新的美术资产或 Live2D 运行时；未来升级只需为语义动作路由增加新的渲染适配器。
- Linux/macOS 的全局热键与开机自启动暂采用明确降级，不引入额外系统依赖；Qt 系统托盘是否可用以实际桌面环境为准。
- 历史 Todo、AutoTask JSON 与用户数据库继续保留，不自动删除；所有迁移保持幂等和非破坏性。
- 阶段1至阶段5的架构改造已形成完整主线，后续工作转为真实桌面验收、性能观测和按需体验迭代，不再保留强制性的下一阶段架构迁移。
