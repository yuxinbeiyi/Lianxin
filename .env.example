# Docker Compose host binding for the headless physical simulator.
# Keep the default loopback binding unless you intentionally need LAN access.
LIANXIN_PHYSICAL_BIND=127.0.0.1
LIANXIN_PHYSICAL_PORT=8765
