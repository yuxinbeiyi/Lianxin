# 贡献指南

1. 使用 `bootstrap.bat` 初始化开发环境，再使用 `run.bat` 启动。
2. 个人配置只存放在 `~/.lianxin/user_config.json`，提交前确认其中内容不在 Git 状态中。
3. 新功能应补充可公开运行的测试；测试数据使用脱敏样本。
4. 不提交聊天记录、运行日志、模型权重、未授权音乐、头像或表情素材。
5. 提交前运行相关测试、`python -m py_compile` 与凭据扫描。
