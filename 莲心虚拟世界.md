# 莲心虚拟世界（LianXin Virtual World）实施方案

> **状态：已确认，作为后续开发的实施基线。**
>
> 本文规定第一版的目标、架构边界、接口、实施顺序和验收标准。后续开发应优先遵守本文；如需改变边界或接口，先更新本文再实现。

## 1. 项目定位

“莲心虚拟世界”不是一次性的贪吃蛇寻食演示，而是莲心未来接入摄像头云台、ESP32、桌面机械臂和 ROS 前的统一数字孪生验证平台。

第一版使用俯视二维房间与三格贪吃蛇，验证以下核心思想：

> LLM 负责理解“想做什么”；本地具身运行时负责决定“如何完成”；渲染器只显示世界，绝不控制世界。

未来增加观察、抓取、巡逻等能力时，尽量复用同一套任务运行时、技能接口、规划器、审计和事件协议；将模拟设备替换为真实设备，而不是把高频控制重新交给 LLM。

## 2. V1 目标与非目标

### 2.1 V1 必须实现

1. 一个可打开的浏览器 Canvas 二维房间：围墙、静态矩形障碍物、三格贪吃蛇、黄色食物、规划路径、状态和日志。
2. 用户可点击地图放置唯一的“活动食物”，并可添加、删除或重置静态障碍物。
3. 手动调试入口：上、下、左、右移动一格，前往食物、取消、急停、重置。
4. 莲心高层工具入口：`navigate_to_marker`、`move_snake`、`cancel_embodied_task`、`get_embodied_status`。
5. 本地 A* 规划：从当前蛇头到食物，避开边界和障碍物。
6. 固定频率的蛇模拟与显式任务状态：成功、无路径、受阻、取消等不能以文本猜测。
7. 浏览器通过 WebSocket 接收只读世界快照和任务事件；前端不拥有权威位置。
8. 将每个来自莲心的具身任务写入既有 `WorkflowStore`，保留参数、状态、耗时、错误和结果摘要。

### 2.2 V1 明确不做

- 不接入 ESP32、机械臂、ROS 或真实电机。
- 不做动态障碍物、实时重规划、传感器噪声、视觉识别或物体抓取。
- 不做路径平滑；V1 保留网格路径，保证可解释、可调试。
- 不让 LLM 输出像素坐标、A* 路径点、速度、PWM 或逐帧指令。
- 不引入通用行为树框架；导航只使用清晰的任务状态机。
- 不迁移或重构现有 `brain/`、浏览器、桌面控制、记忆等模块。

## 3. 架构原则

### 3.1 分层职责

```text
用户文字 / 地图交互
          │
          ▼
Intent 层（AgentCore / LLM）
只表达高层意图，例如 navigate_to_marker(marker_id)
          │
          ▼
Skill 层（NavigationSkill / TurnSequenceSkill）
选择完成策略、提交任务；不直接渲染、不直接写设备协议
          │
          ▼
PAL：Physical Abstraction Layer / 具身运行时
队列、状态机、取消、急停、资源互斥、事件、审计
          │
          ▼
Planner / Controller
A* 路径规划、固定频率路径跟随、碰撞与到达判定
          │
          ▼
DeviceAdapter
V1：SnakeSimulator；未来：ESP32 / 机械臂 / ROS
          │
          ▼
WorldState ──────────► Renderer（浏览器 Canvas，只读订阅）
```

### 3.2 三条不可破坏的规则

1. **唯一权威状态在后端。** `WorldState` 只由 Python 具身运行时修改；浏览器只能渲染其快照。
2. **LLM 只发高层目标。** 例如 `marker_id`，而非坐标或路径；目标真实坐标从后端权威地图读取。
3. **控制循环不依赖 LLM 或渲染帧率。** 后端以固定频率运行；浏览器掉帧、最小化或断连不能改变任务逻辑。

## 4. V1 技术架构

### 4.1 技术选型

| 区域 | 选型 | 原因 |
|---|---|---|
| 具身运行时 | Python 标准库 + `asyncio` | 与莲心现有 Python 后端一致，易于测试与扩展 |
| Web 服务与通信 | `aiohttp` + WebSocket | 项目已有依赖，适合本地页面和状态推送 |
| 路径规划 | 自实现小型 A* 或 `networkx` | V1 地图小，优先可读、可测试、可视化 |
| 画面 | 原生 HTML/CSS/Canvas 2D | 无额外前端框架，足以呈现二维世界 |
| 聊天接入 | 现有 `AgentCore` 工具调用 | 复用现有工具权限、审计和会话链路 |
| 审计 | 现有 `WorkflowStore` | 统一记录任务过程，避免另建黑盒日志 |

### 4.2 运行频率与通信

| 通道 | 频率 | 责任 |
|---|---:|---|
| 后端控制循环 | 50 Hz（固定步长 20 ms） | 更新蛇状态、检查碰撞、推进任务 |
| WebSocket 状态快照 | 50 Hz | 推送只读 `WorldState` 摘要 |
| 浏览器渲染 | `requestAnimationFrame`，通常 60 FPS | 绘制离散蛇格与路径 |
| A* 规划 | 按任务触发 | 地图变化或新导航任务时本地执行 |

浏览器只能影响世界的方式是发送明确的用户交互请求，例如 `place_marker`、`add_obstacle`、`cancel_task`；后端验证后才修改状态。

## 5. 推荐目录与模块边界

```text
brain/physical/
  __init__.py
  models.py             # Point、SnakeState、Obstacle、Marker、TaskStatus
  world.py              # 权威 WorldState 与地图修改接口
  planner.py            # 栅格化、障碍物膨胀、A*
  controller.py         # 固定节拍路径跟随、单格移动、碰撞、到达判定
  runtime.py            # PAL：任务队列、状态机、事件、取消、急停、审计
  simulator.py          # SnakeSimulator：V1 DeviceAdapter
  service.py            # aiohttp 页面服务与 WebSocket 桥接

skills/具身智能模拟/
  SKILL.md              # 能力说明、LLM 使用约束、示例
  tools.py               # 对 AgentCore 暴露的高层工具

gui/physical_sim/
  index.html
  style.css
  app.js                # Canvas、输入、日志、WebSocket；不保存权威状态

tests/
  test_physical_planner.py
  test_physical_runtime.py
  test_physical_controller.py
```

目录是 V1 的推荐组织，而非对旧代码的全局重构要求。现有技能系统支持以 `SKILL.md` 和 `tools.py` 注册工具，应复用此机制。

## 6. 领域模型与接口

### 6.1 权威世界状态

```python
WorldState(
    width=800,
    height=800,
    snake=SnakeState(body=[(180,400), (160,400), (140,400)], direction="right", speed=0),
    obstacles=[...],
    markers={"marker_001": Marker(...)},
    active_marker_id="marker_001",
    active_task=None,
    revision=0,
)
```

- 像素坐标用于渲染和交互；规划时转换为网格坐标。
- V1 网格默认使用 20 px 单元，即 `40 × 40`。
- 蛇初始为三个方格，蛇头红色、身体绿色；吃到食物后本阶段不增长。
- 食物是黄色方格，点击位置自动吸附到 20 px 网格中心。
- 每次地图或任务状态变化递增 `revision`，用于诊断陈旧请求。

### 6.2 高层任务请求

LLM 或手动调试入口只能提交下列结构。坐标由后端解析，不能由 LLM 自行编造。

```json
{
  "kind": "navigate_to_marker",
  "marker_id": "marker_001",
  "objective": "shortest"
}
```

```json
{
  "kind": "turn_sequence",
  "turns": [-90, 90, -90],
  "angular_speed_deg_s": 90
}
```

### 6.3 任务状态机

```text
QUEUED → VALIDATING → PLANNING → PATH_READY → MOVING → ARRIVED
                         │                       │
                         ├──────→ NO_PATH        ├──────→ BLOCKED
                         └──────→ INVALID_GOAL   └──────→ CANCELLED
```

- `QUEUED`：任务已接收，尚未执行。
- `VALIDATING`：验证标记、地图版本、边界与资源互斥。
- `PLANNING`：生成占据栅格并执行 A*。
- `PATH_READY`：已获得路径，等待控制循环接管。
- `MOVING`：控制器持续向下一路径点移动。
- `ARRIVED`：在目标容差内完成。
- `NO_PATH`：不存在可通行路径。
- `INVALID_GOAL`：标记不存在、在边界外或落在障碍物中。
- `BLOCKED`：执行中检测到无法继续（V1 一般只作为防御性状态）。
- `CANCELLED`：用户取消或急停。

每个状态切换必须生成时间戳事件并写入任务审计。

### 6.4 浏览器协议

后端推送：

```json
{
  "type": "world_snapshot",
  "revision": 42,
  "tank": {"x": 280.4, "y": 360.0, "heading_deg": 90},
  "markers": [{"id": "marker_001", "x": 720, "y": 580, "active": true}],
  "obstacles": [{"x": 300, "y": 200, "w": 40, "h": 100}],
  "task": {"id": "...", "status": "MOVING", "progress": 0.46},
  "path": [[...], [...]]
}
```

浏览器请求：

```json
{"type": "place_marker", "x": 720, "y": 580}
{"type": "add_obstacle", "x": 300, "y": 200, "w": 40, "h": 100}
{"type": "start_debug_navigation"}
{"type": "cancel_task"}
{"type": "emergency_stop"}
```

所有请求都由服务端进行范围、碰撞和当前状态校验。

## 7. Skill 与 PAL 的职责约定

### 7.1 Intent 层

`AgentCore / LLM` 只做自然语言到结构化意图的转换。例如：

> “莲心，到我标记的位置。”

调用：

```text
navigate_to_marker(marker_id="marker_001", objective="shortest")
```

LLM 不知道具体路径有多少段、控制器每秒更新多少次，也不应在移动中反复调用工具。

### 7.2 V1 Skill

| Skill | 职责 | V1 实现 |
|---|---|---|
| `NavigationSkill` | 到指定标记点 | 调用 PAL 提交 `navigate_to_marker` |
| `TurnSequenceSkill` | 执行一组相对旋转 | 调用 PAL 提交 `turn_sequence` |

Skill 只能提交高层任务和订阅结果；不得自己持有 Canvas、WebSocket 连接或设备协议细节。

### 7.3 PAL

PAL 是共享运行时，负责：

- 单一活动运动任务，避免两个动作同时抢蛇控制器。
- 任务排队、取消、急停和终态回收。
- 状态机和事件发布。
- 调用规划器、控制器和设备适配器。
- 将任务映射到既有 `WorkflowStore` 审计。
- 向 WebSocket 客户端推送世界快照与事件。

### 7.4 Planner、Controller、DeviceAdapter、Renderer

| 模块 | 能做 | 不能做 |
|---|---|---|
| Planner | 栅格化、A*、无路径诊断 | 直接改 UI、调用 LLM |
| Controller | 根据固定步长更新贪吃蛇 | 调用 LLM、规划整个世界 |
| DeviceAdapter | 模拟或下发设备级命令、返回状态 | 解释用户语言 |
| Renderer | 画地图、路径、贪吃蛇、日志 | 保存或修改权威位置、决定任务成功 |

## 8. V1 交互设计

页面应包含：

1. **地图区**：800×800 院子、围墙、三格贪吃蛇、障碍物、黄色食物和路径。
2. **模式栏**：放置食物、添加障碍、删除障碍、查看路径搜索过程。
3. **任务栏**：状态、任务 ID、蛇头位置、方向、路径长度和当前步骤。
4. **调试控制**：左转、右转、左-右-左、前往标记、暂停、取消、急停、重置。
5. **速度控制**：0.5×、1×、2×、5×、10×；仅影响模拟时间倍率。
6. **日志区**：展示“标记已放置 → 任务已提交 → A* 完成 → 开始移动 → 已到达”及所有失败状态。
7. **聊天联动提示**：显示最近一次由莲心工具提交的任务，但聊天本身仍由现有主界面负责。

## 9. 实施顺序

### 阶段 A：无界面核心（先测试）

1. 定义模型、`WorldState` 和任务状态枚举。
2. 实现地图栅格化、障碍物膨胀和 A*。
3. 实现固定步长贪吃蛇控制器：单格移动、路径点到达和身体碰撞。
4. 实现 PAL：提交、取消、急停、事件和单任务互斥。
5. 编写单元测试：直线路径、绕障碍、无路径、非法目标、取消和急停。

### 阶段 B：可视化与调试

1. 增加 `aiohttp` 静态页面和 WebSocket。
2. 实现 Canvas 地图、三格贪吃蛇、黄色食物、路径与日志。
3. 接入标点、障碍物编辑和调试按钮。
4. 验证页面断开、最小化或掉帧时，后端状态仍连续推进。

### 阶段 C：接入莲心

1. 新增“具身智能模拟”技能的 `SKILL.md` 和 `tools.py`。
2. 添加高层工具定义与执行器，连接 `PhysicalRuntime`。
3. 加入 AgentCore 资源组，确保具身运动任务串行。
4. 将任务生命周期写入 `WorkflowStore`。
5. 使用自然语言验证“到我标记的位置”和“左、右、左”。

### 阶段 D：可靠性演示

1. 故意生成无路可走、目标在障碍物内、任务中取消等场景。
2. 确认莲心收到的是明确终态，而不是猜测动画是否完成。
3. 记录路径规划与任务耗时，形成可回归的演示场景。

## 10. 验收标准

V1 完成必须同时满足：

1. 在含多个静态障碍物的地图中，用户标点后可以稳定规划并抵达；无路径时显示 `NO_PATH`。
2. 贪吃蛇的身体、方向和步频只由后端固定步长控制循环改变；关闭或卡顿浏览器不改变后端逻辑。
3. 手动单格移动直接验证本地控制器，过程中不请求 LLM。
4. “到我标记的位置”只通过一次高层工具调用提交导航，不由 LLM 返回路径点或逐步移动命令。
5. `cancel_task` 与 `emergency_stop` 能在下一个控制周期内停止运动，并以 `CANCELLED` 结束任务。
6. 每个任务在 `WorkflowStore` 中可查询到开始、状态变化、结束、耗时和失败原因。
7. A*、状态机与控制器拥有自动化测试；界面不是正确性的唯一证据。

## 11. 后续迭代路线

### V1.1：导航完善

- 路径简化与线段碰撞校验。
- 多标记点、命名标记和地图保存。
- 动态障碍物导致路径失效时的本地重规划。

### V1.2：感知与观察

- `ObserveSkill`：扫描、虚拟相机、目标检测结果和搜索策略。
- 失败后换角度、扩大扫描范围等有限状态机。
- 将现有 ESP32-CAM 观察能力逐步迁入统一高层任务模型。

### V1.3：操作与行为树

- `PickSkill`：寻找、靠近、对齐、夹取、抬升、验证。
- 此时再引入行为树，用于条件、回退、重试和恢复。
- 保持 Intent、PAL、Planner/Controller 和设备适配器的接口稳定。

### V2：真实设备适配器

- `ESP32Adapter`、`ArmAdapter`、`ROSAdapter`。
- 增加反馈、超时、通信重连、限位、里程计、校准与物理急停。
- 真实设备只能替换 `DeviceAdapter` 与部分 Controller；高层 Skill 不应改写为 LLM 逐步控制。

## 12. 变更纪律

后续每次实现前检查：

1. 该功能属于 Intent、Skill、PAL、Planner、Controller、DeviceAdapter 还是 Renderer？
2. 是否把本应本地闭环的动作错误交给了 LLM？
3. 是否让前端持有或修改了权威世界状态？
4. 是否有明确的任务状态、取消路径、错误码和测试？
5. 是否超出 V1 范围？若超出，先记录到“后续迭代路线”，不抢先堆入 V1。

只有五项都能回答清楚，才进入实现。
