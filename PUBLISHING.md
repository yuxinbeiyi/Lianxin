# 公开发布清单

## 发布阻断项

- [ ] 已撤销曾出现在历史中的第三方 Cookie 与 API Key。
- [ ] 已使用 Gitleaks 或同类工具扫描全部 Git 历史。
- [ ] 已确认 `~/.lianxin`、`data/`、`memory/`、`logs/` 和用户配置未被 Git 跟踪。
- [ ] 已完成音乐、GIF、视觉模型等随仓库发布资源的来源和许可证清单；表情包由用户本地自行配置，不随仓库发布。
- [ ] 已选择并加入项目许可证；未完成前不应公开宣称为开源项目。

## 首发前完善项

- [ ] README 包含安装步骤、系统要求、配置说明、隐私说明和功能边界。
- [ ] 将核心依赖与语音、视觉、QQ/微信、MCP 等可选依赖拆分。
- [ ] 提交脱敏测试并启用 GitHub Actions。
- [ ] 添加凭据扫描、依赖更新和发布前检查。
- [ ] 在干净 Windows 用户环境中运行 `bootstrap.bat` 与 `run.bat` 验收。

## Git 历史瘦身

历史改写需要先处理真实凭据，再使用 `git filter-repo` 清除已失效 Cookie 和不再发布的大型资源。改写后应以干净克隆验证启动，再强制推送远端。协作者需要重新克隆或基于新历史同步。

## Docker 规划

当前首发形态为 Windows PyQt 桌面应用，不将完整 GUI 放入 Docker。后续容器化应只承载无界面服务，例如物理模拟器、任务执行 API 或 Web 服务：

1. 将 GUI 与后台服务拆分为独立入口。
2. 容器默认只绑定 `127.0.0.1`，健康检查只暴露必要端点。
3. 通过 volume 保存数据，通过 `.env` 或 Docker secrets 注入凭据。
4. PyQt 桌面版继续使用本地 `run.bat` 或 PyInstaller 发行包。
