services:
  physical-sim:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      LIANXIN_PHYSICAL_HOST: 0.0.0.0
      LIANXIN_PHYSICAL_PORT: 8765
    ports:
      - "${LIANXIN_PHYSICAL_BIND:-127.0.0.1}:${LIANXIN_PHYSICAL_PORT:-8765}:8765"
